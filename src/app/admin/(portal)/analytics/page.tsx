import { ComingSoon } from "@/components/admin/ComingSoon";

export const dynamic = "force-dynamic";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      note="Attendance trends, batch performance and retention insights arrive in a later phase. Feature-flagged off until then."
    />
  );
}
