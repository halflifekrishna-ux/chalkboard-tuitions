import { ComingSoon } from "@/components/admin/ComingSoon";

export const dynamic = "force-dynamic";

export default function ClassesPage() {
  return (
    <ComingSoon
      title="Classes"
      note="Batch and schedule management (Grade 6 Maths · 5 PM, etc.) arrives alongside Attendance in Phase 2."
    />
  );
}
